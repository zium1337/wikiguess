services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: wikiguess
    ports:
      - "2137:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
